@model IEnumerable<F1_Fantasy_liga.Models.FantasyLeague>

@foreach (var item in Model)
{
    <div class="custom-card clickable-card row-link" data-href="@Url.Action("Details", new { id = item.Id })" tabindex="0" role="link">
        <h3 class="card-title">@item.Name</h3>
        <div class="card-body">
            <div class="card-info-row">
                <span class="card-info-label">Id</span>
                <span class="card-info-value">@item.Id</span>
            </div>
            <div class="card-info-row">
                <span class="card-info-label">Type</span>
                <span class="card-info-value">@item.LeagueType</span>
            </div>
            <div class="card-info-row">
                <span class="card-info-label">Start</span>
                <span class="card-info-value">@item.StartDate.ToString("dd.MM.yyyy")</span>
            </div>
            <div class="card-info-row">
                <span class="card-info-label">End</span>
                <span class="card-info-value">@item.EndDate.ToString("dd.MM.yyyy")</span>
            </div>
        </div>
        <div class="card-actions">
            <a class="details-btn" asp-action="Details" asp-route-id="@item.Id">Details</a>
            <a class="details-btn secondary" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
            <button type="button"
                    class="details-btn danger delete-trigger"
                    data-delete-url="@Url.Action("Delete", new { id = item.Id })"
                    data-entity-name="@item.Name">
                Delete
            </button>
        </div>
    </div>
}
