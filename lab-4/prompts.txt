1.I want to start first with CRUD operation for models but first make for circuit and when it is good i will tell you to make for others. So i added the required and string length fileds on every model and added the soft delete where there is bool for isdeleted and datetime when it was deleted. I want you to make the create page for circuits that has client side validation and in the controller there should be server side validation for the create page. Controller should have GET for the view and POST for when user makes new entity and there should be server side validation. I want to have button on the index page of circuits that says "New circuit" and when pressed it opens the create page for circuits.
2.it is ok just make that the create form is in the middle not on the side. Now i want to make the edit and soft delete for circuits. Because the index has the cards i want to add two buttons on each card "Edit" and "Delete". Edit opens the form for edit but the delete it gives a pop up like are you sure you want to delete "name of select entity" and the pop up has 2 buttons yes and no and if press "yes" it soft deletes the entity or if press "no" the entity is not delete. Also make so that the index page it shows only the entities that are not deleted so it needs to filter. For soft delete in the controller do not use _db.Circuits.Remove(). Instead find the entity, set IsDeleted = true and DeletedAt = DateTime.UtcNow, then call _db.SaveChanges(). In the Index action filter with .Where(c => c.IsDeleted == false).
3.every thing is good just add spacing between the buttons on the cards on the index page
4.Now i want to make the create page for drivers and the index page of drivers will also have the button named "New drivers" that opens the create page. Create page should have also client and server side validation. But for create the drivers have foreign key constructors and i want it to be a standard dropdown where it shows all constructors that are not soft delted and user select which ever he want and the form it sends the id of that constructor. Also make server side validation that checks the driver number is not already taken by another active non-deleted driver.Also include _ValidationScriptsPartial in the view so that client side validation triggers on blur (when field loses focus), not only on submit.
5.edit and the delete same thing like in circuits. On the index page add 2 buttons "Edit" and "Delete" with spacing and when press the delete it should give pop up are you sure you want to delete "entitiy name" in this case driver name and surname should be displayed if pressed "yes" it should soft delete and if pressed "no" it should not do anything. Edit opens the form but it should have for constructors dropdown and make so that index page shows only entites that are not deleted. Edit form should be pre-populated with the existing driver data including the correct constructor already selected in the dropdown.
6.Circuit and Driver CRUD are working and are the reference implementation. Now apply the same pattern to all remaining entities: Race, RaceResult, Constructor, FantasyLeauge, FantasyTeam, User. For Race, Constructor, FantasyLeague and FantasyTeam they use card layout like Circuit and Driver so add the "Edit" and "Delete" buttons on each card and the same pop up confirm with entity name when user presses button to "Delete" which is soft delete. For User and RaceResult they use table layout on index page so add two new columns "Edit" and "Delete" at the end of each table row that have buttons and "Delete" is still soft delete where pop up confirmation dialog. The "Edit" button for every one stays the same it opens the edit page for that entity. Every entity index page have the create button same like in Circuit and Drivers index pages.
Foreign key dropdowns for each entity:
Race has Circuit dropdown shows only non-deleted
RaceResult has Driver and Race dropdown shows only non-deleted
FantasyTeam has User, Constructor and FantasyLeague dropdown shows only non-deleted
Constructor, FantasyLeague, User do not have foreign keys.
On the create page for Constructor it does not need the select for drivers because on drivers create form there is the select the constructor also there is no need for FantasyLeague and USer to have select for Fantasy teams beacuse it is handled in create form of fantasy team.
For FantasyTeam create form include only name, budget and dropdowns for user, construtctor and fantasy league. Same thing should be on the edit form but also on edit form add button "Manage drivers" that opens a separate page where user can add and remove drivers from the team using a two column layout available drivers on left and selected on right with remaining budget counter that updates dynamicaly with Javascript as drivers are added or removed and can't select the same driver and drivers already on the tema should apper on the right side when Manage Drivers page loads. Base this on the existing BuildTeam page pattern.
All edit forms must be pre-populated with existing data including correct dropdown selection. All index pages must filter out soft deleted entities. There needs to be server side and client side validations. Client side validation must trigger on blur (when field loses focus) using _ValidationScriptsPartial, not only on submit.
7.Fix the following issues:

SOFT DELETE FILTERING: In all Details and Index pages, filter out soft deleted related entities. Specifically: Constructor Details should not show deleted drivers. FantasyLeague Details leaderboard should not show deleted teams. User Details should not show deleted teams. Race Details should not show deleted race results. FantasyTeam Details should not show deleted drivers or constructors. FantasyTeams whose User is soft deleted should not appear on FantasyTeam Index.
CASCADE FILTERING: If a parent entity is soft deleted, hide its children. Examples: if User is soft deleted their FantasyTeams should not appear anywhere. If Race is soft deleted its RaceResults should not appear anywhere. If Driver is soft deleted their RaceResults should not appear anywhere.
FANTASYTEAM EDIT: Fix the Edit form so that User, Constructor and FantasyLeague dropdowns are pre-populated with the current values.
RACERESULT CREATE/EDIT: Remove manual input for ScoredPoints — it should be calculated automatically on the server using the existing RaceResultPoints enum (First=25, Second=18, Third=15, Fourth=12, Fifth=10, Sixth=8, Seventh=6, Eighth=4, Ninth=2, Tenth=1, OutOfPoints=0). The DriverStatus dropdown already exists. FinishedPosition remains as a manual input. If status is not Finished set ScoredPoints=0. If status is Finished calculate ScoredPoints from FinishedPosition using RaceResultPoints enum — if position > 10 set ScoredPoints=0.
USER CREATE/EDIT: Add a ConfirmPassword field on both Create and Edit forms. Add server side validation that ConfirmPassword matches PasswordHash field. Do not save ConfirmPassword to database.
FANTASYLEAGUE CREATE/EDIT: Add server side validation that EndDate is not before StartDate. Show error message on EndDate field if invalid.
8.when i try to make a build i get error cs1061 - 'ClaimsPrinicipal' does not contain a definition for 'ConfrimPassword' and no accessible extension method 'ConfrimPassword' accepting a first argument of type 'ClaimsPrincipal' could be found(are you missing a  using directive or an assembly reference) on the lines 42 and 106 in file UsersControllers.cs
9.Now i want to add the AJAX search on all index pages for every entity but first start with race results because it already has search but make it so it is now AJAX search so that the table updates without page refresh as the user types. So it returns filtered non-deleted race results matching the entered term.
10.Ok Race results AJAX search works great and is reference implementation. Now add the AJAX search for other entities where it return filtered non-deleted results matching the entered term. On Drivers index search is for their name and surname combined, Constructors search is for their constructor name, circuit for circuit name, fantasy league is name of fantasy league, fantasy team name of fantasy team, race is name of race, users search for their name and surname combined. Everyone is using cards only users uses the table.
11.Now i want to make dropdown with autocomplete where autocomplete must use AJAX for asynchronously accept results from server. I would want it on the create and edit pages where model has foreign key. Drivers create and edit have dropdown for constructors, Races have dropdown for circuits, race results have dropdowns for circuit and driver and fantasy team have dropdown for user, constructor and fantasy league and on their create and edit pages i want to add the dropdown with autocomplete where it uses AJAX for asynchronous accepting results from server. Make a model for autocomplete so that can be reused. 
12.Now when the home page loads, animate the info cards so they slide in from the bottom one by one with a staggered delay using CSS transitions triggered by JavaScript.
13.FANTASY LEAGUE DETAILS - PODIUM ANIMATION: On the FantasyLeague Details page show the top 3 teams by points as an F1 podium before the full leaderboard table. Second place is on the left at medium height, first place is in the center at the greatest height, third place is on the right at the lowest height. Each podium block animates upward from the bottom when the page loads with a staggered delay. The podium blocks should show team name and points.
14.Add a custom date and time picker as a partial view without using the browser's default date input (do not use input type="date" or input type="datetime-local"). Implement it completely in vanilla JavaScript.
Create a model class DatePickerViewModel in folder Models. Create Views/Shared/_DatePicker.cshtml as a reusable partial view. The picker must show separate inputs or a calendar popup for date and time (hours and minutes). Detect the user's browser language using navigator.language in JavaScript — if language starts with 'hr' display date as dd.MM.yyyy, for English locales display as MM/dd/yyyy. Time is always HH:mm. Apply the date picker partial view to all existing forms that use date fields: Race Create and Edit (RaceDate), Constructor Create and Edit (FoundedDate), FantasyLeague Create and Edit (StartDate and EndDate).
15.